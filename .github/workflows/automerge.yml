name: automerge
on:
  pull_request:
jobs:
  automerge:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: reitermarkus/automerge@v2
        with:
          token: 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
          merge-method: rebase
          do-not-merge-labels: never-merge
          required-labels: automerge
          pull-request: ${{ github.event.inputs.pull-request }}
          review: ${{ github.event.inputs.review }}
          dry-run: true
